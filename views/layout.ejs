<!DOCTYPE html>
<html lang="uk">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title || 'DZ63' %></title>
    <link rel="stylesheet" href="/css/main.css">
  </head>
  <body class="ui">
    <header class="classy-header">
      <div class="classy-header__inner container">
        <a class="classy-header__brand" href="/"><strong>DZ63</strong></a>
        <nav class="classy-header__nav">
          <a class="classy-header__link" href="/users">Користувачі (demo)</a>
          <a class="classy-header__link" href="/protected">/protected</a>
          <% if (user) { %>
            <form action="/auth/logout" method="post" class="inline">
              <button class="btn" type="submit">Вийти (<%= user.email %>)</button>
            </form>
          <% } else { %>
            <a class="btn" href="/auth?tab=login<%= path && path !== '/' ? ('&next=' + encodeURIComponent(path)) : '' %>">Увійти</a>
          <% } %>
        </nav>
      </div>
    </header>
    <main class="container">
      <%- body %>
    </main>
    <footer class="footer">
      <p class="footer__p">© <%= new Date().getFullYear() %> Demo</p>
    </footer>
    <% if (typeof extraScripts !== 'undefined') { %><%- extraScripts %><% } %>
  </body>
</html>
